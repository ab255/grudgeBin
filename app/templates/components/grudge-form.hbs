<form>
  <label>
    Person:
    {{input class='spec-name-input' value=grudge.name}}
  </label>
  <label>
    Wrongs:
    {{textarea class='spec-wrongs-input' value=grudge.wrongs}}
  </label>
  <label>
    Forgiven:
    {{input type='checkbox' class='spec-forgiven-input' checked=grudge.forgiven}}
  </label>
  <button type="submit" class="spec-submit-grudge">Submit</button>
  {{yeild}}
</form>
